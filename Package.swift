// swift-tools-version: 6.0

import PackageDescription

let package = Package(
    name: "KDBXSync",

    dependencies: [
      // ecternal package
    ],

    targets: [
        .executableTarget(
            name: "KDBXSync",
            dependencies: [
                // packages used by this executable
            ]
        )
    ]
)
